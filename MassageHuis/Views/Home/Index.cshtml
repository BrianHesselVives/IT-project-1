@using MassageHuis.ViewModels
@model MassageHuis.ViewModels.MasseurVM
@if (Model != null)
{
    <div class="alert alert alert-success alert-dismissible fade show" role="alert">
        Massage succesvol gereserveerd op @Model.vrijeSlots.FirstOrDefault()
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}